spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true

# starts the MySQL service from compose.yaml and runs the application against it; must be set here
# explicitly, because application.properties switches Docker Compose off for all other profiles.
# Compose also supplies url, user and password, so this profile declares no spring.datasource.* of
# its own - with Compose off it would have no database to connect to.
spring.docker.compose.enabled=true

# on shutdown, remove the container instead of only stopping it ('docker compose down' instead of
# 'docker compose stop'); '-v' also removes its data volume, so no orphaned volumes pile up.
# Every start therefore begins with an empty database: Flyway migrates and BootstrapData seeds it again.
# Only happens on a graceful shutdown (Ctrl+C / IDE stop) - a killed process runs no shutdown hook.
spring.docker.compose.stop.command=down
spring.docker.compose.stop.arguments=-v

spring.datasource.hikari.pool-name=RestDB-Pool
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.data-source-properties.cachePrepStmts=true
spring.datasource.hikari.data-source-properties.prepStmtCacheSize=250
spring.datasource.hikari.data-source-properties.prepStmtCacheSqlLimit=2048
spring.datasource.hikari.data-source-properties.useServerPrepStmts=true
spring.datasource.hikari.data-source-properties.useLocalSessionState=true
spring.datasource.hikari.data-source-properties.rewriteBatchedStatements=true
spring.datasource.hikari.data-source-properties.cacheResultSetMetadata=true
spring.datasource.hikari.data-source-properties.cacheServerConfiguration=true
spring.datasource.hikari.data-source-properties.elideSetAutoCommits=true
spring.datasource.hikari.data-source-properties.maintainTimeStats=false


#Show SQL
spring.jpa.properties.hibernate.show_sql=true

#Format SQL
spring.jpa.properties.hibernate.format_sql=true

# SQL-Syntax Highlighting
spring.jpa.properties.hibernate.highlight_sql=true

#Show bind values
logging.level.org.hibernate.orm.jdbc.bind=trace