# Test-only profile for tests running against a MySQL Testcontainer.
# The container supplies url, username and password (see MySqlContainerBase),
# so this profile only switches on what those tests need.
spring.flyway.enabled=true
spring.jpa.hibernate.ddl-auto=validate
